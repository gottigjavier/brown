---
import Section from "./Section.astro"
---

<Section
  color="white"
  id="pantalla-1"
  texto="Faro Marina do Brasil. Brasil"
>
<img
slot="background"
alt="Pantalla 1"
class="h-full w-full object-cover object-center bg-image"
src="brown06.avif"
id="bg-image-pantalla-1"
/>
</Section>
<!-- 
<script>
  function isEdge() {
    if (
      navigator.userAgent.indexOf("Edge") > -1 ||
      navigator.userAgent.indexOf("Edg") > -1
    ) {
      return true
    } else {
      return false
    }
  }

  let imageElement = document.querySelector(
    "#bg-image-modely"
  ) as HTMLImageElement

  if (isEdge()) {
    imageElement.src = "brown1.jpg"
  } else {
    imageElement.src = "brown1.jpg"
  }
</script>
 -->