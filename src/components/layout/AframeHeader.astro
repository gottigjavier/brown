---
const { color } = Astro.props;
const textColor = `text-${color}`;
---

<header
  id="landing-header"
  class=`py-1 px-2 flex items-center fixed top-0 z-40 ${textColor} overflow-x-hidden overflow-y-hidden w-full`
>
  <nav class="logo">
    
      <div id="drone">
        <img src="/backgrounds/flydrone-solo.gif" alt="gif" class="drone-float">
      </div>
      <div id="logo">
        <video
          id="video-earth"
          muted 
          autoplay
          loop 
          src="/backgrounds/dronvideonosus720.mp4">
        </video> 
      </div>

      <div class="text-container">
        <p id="logo-text">Grabación, edición y producción de imágenes para turismo, hotelería, empresas.</p>
        <p id="logo-text1">Fotografías 360º - Videos 4k</p>
      </div>
    
  </nav>

  <!-- Mobile hamburger -->
  <button id="hamburger" class="sm:hidden z-50 p-2 fixed top-2 right-2" aria-label="Menú">
    <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" class="text-white"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
  </button>

  <nav class="hidden sm:flex flex-grow justify-end basis-0 md:m-5">
    <ul
      class="nav-menu text-sm [&>li>a]:transition-colors [&>li>a]:duration-500 [&>li>a]:text-current [&>li>a]:font-medium [&>li>a]:py-2"
    >
      <li class="btn-menu">
        <a href="/inicio" class="saber-mas-btn">
          Saber más
        </a>
      </li>
      <li id="video" class="menu-img">
          <video muted autoplay loop src="/backgrounds/fabrega-text.mp4">
          </video>
      </li>
      <li id="city" class="menu-img">
        <img src="/backgrounds/city-nocturna.avif" alt="Inmobiliarias">
      </li>
      <li id="obelisco" class="menu-img">
        <img src="/backgrounds/obelisco-t.avif" alt="Empresariales">
      </li>
      <li id="parana" class="menu-img">
        <img src="/backgrounds/parana-costa.avif" alt="Turismo">
      </li>
    </ul>
  </nav>

  <!-- Mobile menu overlay -->
  <div id="mobile-menu" class="fixed inset-0 bg-black/95 backdrop-blur-lg z-40 hidden flex-col items-center justify-center gap-8">
    <a class="text-white text-2xl tracking-wider" href="/inicio">Inicio</a>
    <a class="text-white text-2xl tracking-wider" href="/galeria">Galería</a>
    <a class="text-white text-2xl tracking-wider" href="/trabajos">Realizaciones</a>
    <a class="text-white text-2xl tracking-wider" href="/inicio">Contacto</a>
  </div>

  <img id="degree-360" src="/icons/360-degrees.png" alt="360 icon">
  
  <div
  id="menu-backdrop"
  class={`
      absolute bg-white/5 backdrop-blur-lg rounded
      translate-x-[var(--left)] translate-y-[var(--top)]
      left-0 top-0
      w-[var(--width)] h-[var(--height)]
      transition-all duration-500
      ease-in-out opacity-0 -z-10
      `}
  >

</div>
</header>

<script>
  const listItem = document.querySelectorAll("#landing-header li")
  const menuBackDrop = document.querySelector("#menu-backdrop") as HTMLElement

  listItem.forEach((item) => {
    if (item.classList.contains('btn-menu')) return;
    item.addEventListener("mouseenter", () => {
      const { left, top, width, height } = item.getBoundingClientRect()

      menuBackDrop.style.setProperty("--left", `${left}px`)
      menuBackDrop.style.setProperty("--top", `${top}px`)
      menuBackDrop.style.setProperty("--width", `${width-1}px`)
      menuBackDrop.style.setProperty("--height", `${height-1}px`)

      menuBackDrop.style.opacity = "1"
      menuBackDrop.style.visibility = "visible"
    })

    item.addEventListener("mouseleave", () => {
      menuBackDrop.style.opacity = "0"
      menuBackDrop.style.visibility = "hidden"
    })
  })


  const listImg = document.querySelectorAll(".menu-img")
  const tetxCont = document.querySelector(".text-container") as HTMLElement
  let stype = screen.orientation.type || '';
  screen.orientation.addEventListener("change", (event) => {
    stype = event.target['type'];
    hiddenText(stype);
  });

  function hiddenText(stype: string){
    if (stype === 'landscape-primary'){
      listImg.forEach((imgItem) => {
      imgItem.addEventListener("mouseenter", () => {
        tetxCont.style.opacity = "0"
        
      })
      
      imgItem.addEventListener("mouseleave", () => {
        tetxCont.style.opacity = "1"
        
      })
    })
  }
};
hiddenText(stype);

  // Hamburger toggle
  const hamburger = document.getElementById('hamburger');
  const mobileMenu = document.getElementById('mobile-menu');
  if (hamburger && mobileMenu) {
    hamburger.addEventListener('click', () => {
      const isOpen = !mobileMenu.classList.contains('hidden');
      mobileMenu.classList.toggle('hidden', isOpen);
      mobileMenu.classList.toggle('flex', !isOpen);
      hamburger.innerHTML = isOpen
        ? `<svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" class="text-white"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>`
        : `<svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" class="text-white"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>`;
    });

    mobileMenu.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        mobileMenu.classList.add('hidden');
        mobileMenu.classList.remove('flex');
        hamburger.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" class="text-white"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>`;
      });
    });
  }
</script>


<style>


.logo {
    position: fixed;
    width: 25%;
    top: 12%;
    left: 8%;
  }

  #video-earth {
    clip-path: ellipse(46.5% 47% at 48% 48.5%);
    transform: skew(3deg) scale(0.7);
    object-fit: cover;
    width: 100%;
  }

  
  #drone {
    z-index: 90;
    position: fixed;
    width: 30%;
    top: 62%;
    left: 13%;
  }

  .drone-float {
    animation: droneFloat 3s ease-in-out infinite;
  }

  @keyframes droneFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
  }

  .text-container {
    width: 45%;
    text-align: center;
    position: fixed;
    transition-duration: 500ms;
    color: rgb(253, 211, 96);
    font-weight: 500;
    font-size: 1.8rem;
    top: 20vw;
    right: 15%;
    text-wrap: pretty;
    text-shadow: -1px -1px 1px #b96300, 1px -1px 1px #333, -1px 1px 1px #333, 1px 1px 1px #333;
  }

  .text-container:hover {
    transition-duration: 500ms;
    color: rgb(253, 211, 96);
  }

  .saber-mas-btn {
    display: inline-block;
    padding: 0.6rem 1.5rem;
    background: rgba(255,255,255,0.08);
    backdrop-filter: blur(8px);
    border-radius: 8px;
    font-weight: 600;
    letter-spacing: 0.5px;
    transition: all 0.3s ease;
  }

  .saber-mas-btn:hover {
    background: rgba(255,255,255,0.18);
    box-shadow: 0 0 20px rgba(253, 211, 96, 0.15);
  }

  #degree-360 {
    width: 3%;
    position: fixed;
    left: 3%;
    top: 3%;
  }

  .btn-menu {
    font-size: 1.1vmax;
    position: fixed;
    right: 1%;
  }


  .menu-img {
    transition-duration: 500ms;
    position: fixed;
  }
  .menu-img:hover, .menu-img:active {
    transition-duration: 500ms;
    z-index: 50;
   }

  #video {
    width: 15vmin;
    height: 9vmin;
    top: 16%;
    right: 1%;
  }
  #city {
    width: 15vmin;
    height: 9vmin;
    top: 26.5%;
    right: 1%;
  }

  #obelisco {
    width: 15vmin;
    height: 9vmin;
    top: 37.5%;
    right: 1%;
  }

  #parana {
    width: 15vmin;
    height: 9vmin;
    top: 48.5%;
    right: 1%;
  }


  #video:hover, #video:active {
    width: 64%;
    top: 15%;
    right: 0.9%;
  }

  #city:hover, #city:active {
    width: 64%;
    top: 15%;
    right: 0.9%;
  }

  #obelisco:hover, #obelisco:active {
    width: 64%;
    top: 15%;
    right: 0.9%;
  }

  #parana:hover, #parana:active {
    width: 64%;
    top: 15%;
    right: 0.9%;
  }

  @media (orientation: portrait) {

    .logo {
    position: fixed;
    width: 48%;
    top: 8%;
    left: 22%;
  }

  #logo {
    padding: 2%;
  }

  
  #drone {
    position: fixed;
    width: 25%;
    top: 52%;
    left: 41%;
  }

  .text-container {
    width: 50%;
    text-align: center;
    position: fixed;
    transition-duration: 500ms;
    color: rgb(253, 211, 96);
    font-weight: 500;
    font-size: 2vh;
    top: 42%;
    left: 20%;
    text-wrap: pretty;
    text-shadow: -1px -1px 1px #b96300, 1px -1px 1px #333, -1px 1px 1px #333, 1px 1px 1px #333;
  }

  .text-container:hover {
    transition-duration: 500ms;
    color: rgb(253, 211, 96);
  }


    .btn-menu {
    font-size: 1.5vmax;
    position: fixed;
    right: 0%;
    top: 2%;
  }

  .menu-img {
    transition-duration: 500ms;
    position: fixed;
  }
  .menu-img:hover, .menu-img:active {
    transition-duration: 500ms;
    z-index: 50;
   }

   #degree-360 {
    width: 5%;
    position: fixed;
    left: 3%;
    top: 3%;
  }

  #video {
    width: 15vmin;
    height: 9vmin;
    top: 26%;
    right: 1%;
  }
  #city {
    width: 15vmin;
    height: 9vmin;
    top: 32.5%;
    right: 1%;
  }

  #obelisco {
    width: 15vmin;
    height: 9vmin;
    top: 39.5%;
    right: 1%;
  }

  #parana {
    width: 15vmin;
    height: 9vmin;
    top: 46.5%;
    right: 1%;
  }

  #video:hover, #video:active {
    width: 76%;
    top: 65%;
    right: 11%;
  }

  #city:hover, #city:active {
    width: 76%;
    height: 50%;
    top: 65%;
    right: 11%;
  }

  #obelisco:hover, #obelisco:active {
    width: 76%;
    height: 50%;
    top: 65%;
    right: 11%;
  }

  #parana:hover, #parana:active {
    width: 76%;
    height: 50%;
    top: 65%;
    right: 11%;
  }

}

@media (max-height: 460px) and (orientation: landscape) {

  .logo {
    position: fixed;
    width: 27%;
    top: 6%;
    left: 7%;
  }

  #logo {
    padding: 5%;
  }

  #drone {
    position: fixed;
    width: 12%;
    top: 8%;
    left: 55%;
  }

  .text-container {
    width: 45%;
    text-align: center;
    position: fixed;
    transition-duration: 500ms;
    color: rgb(253, 211, 96);
    font-weight: 500;
    font-size: 1rem;
    top: 37%;
    left: 39%;
    text-wrap: pretty;
    text-shadow: -1px -1px 1px #b96300, 1px -1px 1px #333, -1px 1px 1px #333, 1px 1px 1px #333;
  }

  .text-container:hover {
    transition-duration: 500ms;
    color: rgb(253, 211, 96);
  }

  .btn-menu {
    font-size: 0.9vmax;
    position: fixed;
    right: 1%;
  }

  .saber-mas-btn {
    padding: 0.3rem 0.8rem;
    font-size: 0.9em;
  }


  .menu-img {
    transition-duration: 500ms;
    position: fixed;
  }
  .menu-img:hover, .menu-img:active {
    transition-duration: 500ms;
    z-index: 50;
   }

  #video {
    width: 15vmin;
    height: 9vmin;
    top: 16%;
    right: 1%;
  }
  #city {
    width: 15vmin;
    height: 9vmin;
    top: 26.5%;
    right: 1%;
  }

  #obelisco {
    width: 15vmin;
    height: 9vmin;
    top: 37.5%;
    right: 1%;
  }

  #parana {
    width: 15vmin;
    height: 9vmin;
    top: 48.5%;
    right: 1%;
  }


  #video:hover, #video:active {
    width: 50%;
    top: 25%;
    right: 12%;
  }

  #city:hover, #city:active {
    width: 50%;
    top: 25%;
    right: 12%;
  }

  #obelisco:hover, #obelisco:active {
    width: 50%;
    top: 25%;
    right: 12%;
  }

  #parana:hover, #parana:active {
    width: 50%;
    top: 25%;
    right: 12%;
  }


}

</style>
